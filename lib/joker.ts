/* Joker feature removed. Stub kept for backward compatibility with any
 * cached imports — does nothing. Safe to delete once nothing imports it. */
export interface JokerUsage {
  perStage: Record<string, number>;
  lastUsedAt: number;
}
export {};
