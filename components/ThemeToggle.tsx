/* Theme toggle removed — app is always dark mode.
 * Kept as a no-op stub so any stale import won't break the build.
 * Safe to delete from disk. */
export default function ThemeToggle() { return null; }
